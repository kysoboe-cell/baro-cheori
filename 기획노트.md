# 기획노트.md — 바로처리 (barocheori.com)

Claude(채팅)와 유석 님의 기획·전략 대화 기록.
CLAUDE.md가 "기술/현재상태"를 담당한다면, 이 파일은 "왜 그렇게 하기로 했는지"를 담당한다.
새 채팅을 시작할 때 이 파일을 첨부하면 기획 맥락이 바로 이어진다.

**사용법**: "md로 남겨"라고 말하면 그날의 작업 내용 + 대화 핵심을 이 파일에 追加 갱신한다.

---

## 운영자 프로필 (변하지 않는 정보)

- 유석 님, 1인 운영, 코딩 경험 없음 (바이브 코딩)
- 항상 최종 승인자/관리자 역할. 실행은 Claude(채팅) + Claude Code(VS Code)가 나눠서 담당
- 목표: Claude를 통한 자동화 시스템 구축. Claude = 코딩직원 + 기획직원
- 자금 사정 빠듯함 → 유료 결정은 항상 금액·이유·무료 대안 먼저 제시 후 승인
- 시간이 급함 — "빨리 최대한 빨리 수익화"가 최우선 동기. 조급함 관리가 중요한 리스크.
- 지금 프로젝트에 국한되지 않고, 앞으로 여러 개의 수익 사이트를 만들 계획

## 역할 구조 (합의된 비유)

- **Claude(웹 채팅)** = 기획실장. 조사·설계·전략, 브라우저 자동화(Claude in Chrome)로 서치콘솔/GA4/도메인 등록 같은 대시보드 작업 수행.
- **Claude Code(VS Code)** = 시공기사. 실제 코드 수정·빌드·배포·컴퓨터 접근을 수행. 항상 허용(permission) 알림을 통해 유석 님 승인을 받고 진행.
- **유석 님** = 사장님. 결재·승인만. 코드는 절대 직접 안 고침.

## 개발 도구 세팅 (2026-08-19)

- MCP 도구 2개를 user 범위로 등록 → 앞으로 만들 모든 프로젝트에 자동 적용됨
  - **playwright**: Claude Code에게 "눈"을 달아줌 — 실제 브라우저를 직접 열어서 사이트가 진짜 작동하는지 검증. 처음엔 엣지 창을 띄우는 방식으로 설정했다가, 유석 님이 웨일을 써야 하는데 엣지가 자꾸 뜨는 문제가 생겨 `--headless`(창 안 뜨고 백그라운드 실행)로 변경.
  - **context7**: "최신 기술 커닝페이퍼" — 옛날 지식으로 코딩 실수하는 걸 방지, 최신 공식 문서를 실시간 조회.
- CLAUDE.md: 프로젝트 기술 지침서. Claude Code가 매 세션 자동으로 읽음. 기존 AGENTS.md 내용 등을 병합해 새로 작성, 이후 인수인계서(PDF) 대신 이 파일이 "살아있는 문서" 역할을 함.
- 이 기획노트.md: 기획/전략 쪽 기억 저장소. CLAUDE.md와 역할 분담.
- 도구는 필요할 때 하나씩 추가하는 방식으로 간다 (미리 잔뜩 설치 안 함 — 1인 기업 운영 원칙).

## 프로젝트 상태 흐름 (요약 타임라인)

- **~2026-08-19 오전**: 도메인(barocheori.com) 이전 패치 배포 완료, 구글 서치콘솔·GA4 연동 완료.
- **2026-08-19 저녁**: 사이트에서 링크 클릭 무반응 사고 발생 → 원인 진단 시작.
  - 1차 진단: Cloudflare Workers 무료 플랜 CPU 한도(요청당 10ms) 초과로 503 에러. 원인 후보 = 링크 많은 페이지에서 Next.js 프리페치가 동시다발로 나가는 것.
  - 2차 진단(더 정확한 근본 원인): 141개 페이지가 빌드 때 미리 만들어졌지만 캐시 설정이 비어있어("dummy") 실제로는 매 요청마다 서버가 페이지를 처음부터 다시 렌더링하고 있었음. 여기에 프리페치 폭주가 겹쳐 CPU 한도를 넘김.
  - 수리: `open-next.config.ts`에 `staticAssetsIncrementalCache` + `enableCacheInterception` 추가(미리 만든 페이지를 정적 자산으로 그대로 서빙) + 링크 25곳에 `prefetch={false}` 추가(가장 큰 범인은 전 페이지 하단 SiteFooter의 업체 23개 링크).
  - 검증: 실제 브라우저로 링크 클릭 테스트 + 동시요청 40개 부하 테스트 모두 통과(503 0건). 배포 완료.
- **같은 날**: 넷리파이(baro-cheori.netlify.app) 사이트 삭제 완료 — 구글 신호 분산 문제 해결. 사이트가 GitHub에 연결돼 있어 몰래 자동배포하던 문제도 같이 해결됨.
- **같은 날**: 커밋 3개로 기록 정리(도메인 전환/503 응급수리/문서 갱신) + GitHub 푸시 완료. CLAUDE.md도 이번 작업 내용으로 갱신.

## 남은 할 일 — "청소의 날" 나머지 (다음 세션 우선순위)

CLAUDE.md §6-A 기준, 아직 완료 안 된 것:
1. 구글 서치콘솔: barocheori.com 도메인 속성 신규 등록 + sitemap 제출
2. 네이버 서치어드바이저: 소유확인 버튼은 눌렀음(진행 중) → 사이트맵 제출까지 마무리
3. 다음(Daum) 검색등록
4. GA4 스트림 URL 갱신 + 새 서치콘솔 속성과 연동

청소의 날 이후 = 콘텐츠 보강(검색량 큰 20~30페이지) → 애드센스 신청 → 쿠팡파트너스/제휴 CPA.

## 참고: 알아두면 좋은 리스크 신호

- Cloudflare Git 자동 빌드는 원래부터 고장 상태 — GitHub에 푸시해도 자동빌드 실패 알림이 뜰 수 있지만 무시해도 됨. 실제 배포는 항상 `npm run deploy`로만.
- 무료 플랜 한도(요청/일, CPU 시간)는 현재 트래픽 대비 여유 많음 — 유료 전환은 급하지 않음.
