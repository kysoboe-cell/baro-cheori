# CLAUDE.md — 바로처리 (barocheori.com)

Claude Code가 이 프로젝트에서 작업할 때 자동으로 읽는 지침 파일.
프로젝트 루트(baro-cheori 폴더 맨 위)에 둔다. 최종 갱신: 2026-08-21

옛 기획노트(D:\Downloads\기획노트.md, "Claude 채팅"과의 기획 대화 기록)는 이 문서로 통합됨. 더 이상 갱신하지 않음.

---

## 0. 최신 상태 요약 (매 작업 종료 시 갱신 — §1 습관 규칙 참고)

### 운영자 프로필
- 유석 님, 1인 운영, 코딩 경험 없음(바이브 코딩). 항상 최종 승인자.
- Claude = 코딩직원 + 기획직원. 목표는 Claude를 통한 자동화 시스템 구축.
- 자금 사정 빠듯함 → 유료 결정은 항상 금액·이유·무료 대안 먼저 제시 후 승인.
- "빨리 최대한 빨리 수익화"가 최우선 동기 — 조급함 관리가 중요한 리스크(결과를 과장하지 않기).
- 지금 프로젝트에 국한되지 않고, 앞으로 여러 개의 수익 사이트를 만들 계획.

### 역할 구조
- **Claude(웹 채팅)** = 기획실장. 조사·설계·전략, 브라우저 자동화로 서치콘솔/GA4/도메인 등록 같은 대시보드 작업 수행.
- **Claude Code(VS Code)** = 시공기사. 실제 코드 수정·빌드·배포.
- **유석 님** = 사장님. 결재·승인만. 코드는 절대 직접 안 고침.
- **프리패쓰 정책**(2026-08-19 선언): 돈 안 드는 작업은 Claude가 알아서 판단하고 진행(사소한 확인 질문 최소화). 유료/과금 결정만 사전 승인 필수. 단, 결제·메시지 발송·약관 동의·양식 제출 등은 그때그때 확인 필요(§1 안전 규칙과 동일 선상).
- **Bypass permissions 모드 켬**(2026-08-20 결정): Claude Code가 앞으로 도구 실행 시 승인 팝업 없이 자동 진행. 단 §1의 유료 결정·파괴적 작업(DNS·도메인·계정 설정 변경 등) 사전 설명·승인 원칙은 이 모드와 별개로 그대로 유효 — 팝업이 안 뜬다고 판단 없이 진행하지 않기.

### 현재 진행 상황
- 완료: Workers 이전 → 도메인(barocheori.com) 전환 배포 → 503 링크 먹통 응급수리 → 넷리파이(baro-cheori.netlify.app) 사이트 삭제 → 검색엔진 등록 3사(네이버 서치어드바이저·구글 서치콘솔·다음 검색등록) 완료 → GA4 실시간 수집 확인 → 옛 workers.dev 주소 완전 폐쇄(2026-08-20, `workers_dev: false` 추가 → 빌드 → 배포 → 브라우저 실측 검증까지 완료, 구글 신호 분산 문제 완전 해결) → 기획노트.md·CLAUDE.md 문서 통합 완료(2026-08-20, 기획노트.md는 폐기 표시만 남기고 보존, 이 "0. 최신 상태 요약" 섹션 신설 + 매 세션 종료 시 갱신하는 습관 규칙 명문화) → Claude Code Bypass permissions 모드 켬(2026-08-20) → **가전 Tier1 콘텐츠 보강 완료(2026-08-20)**: 삼성전자서비스·LG전자의 `repair-cost-warranty`(수리비·보증기간 확인) 2페이지에 "품목별 대략 수리비 참고표" + FAQ 3개, `service-center`(서비스센터 찾기·방문예약) 2페이지에 FAQ 3개 추가. `ServiceTask` 타입에 `priceTable`/`faq` 필드 신설, FAQPage JSON-LD 스키마도 함께 삽입(검색결과 아코디언 노출 노리는 용도). 출장비 수치는 삼성전자서비스·LG전자 공식 요금 산정기준 페이지로 대조해 보정(공식 정액 출장비 2.5만~3.8만원대), 품목별 수리비는 공식 페이지에 없는 대략치라 표에 명시. 빌드→배포→브라우저 4페이지 실측 검증(콘텐츠 노출, 콘솔 오류 0, JSON-LD 파싱 정상)까지 완료.
- **Tier1 콘텐츠 보강 나머지 14페이지 완료(2026-08-20)**: 쇼핑몰 반품·환불 4개(쿠팡·스마트스토어·G마켓·11번가)에 "환불 소요기간 참고표" + FAQ, 구독 환불 7개(넷플릭스·유튜브·Apple·Google Play·티빙·웨이브·디즈니+)와 카드 분실 3개(KB·신한·삼성카드)에 FAQ 추가. 전화번호가 이미 있는 페이지(쿠팡·KB국민카드 등)는 새로 안 넣음. 작업 중 버그 발견·즉시 수정: 가전 수리비 표 컴포넌트를 재사용하면서 헤더가 "제품/흔한 고장 유형/출장비" 로 고정돼 있어 쇼핑몰 환불표에 문맥이 안 맞았음 → `ServiceTask`에 `priceTableHeading`(섹션 제목·열 이름 커스터마이즈) 필드 신설 + `visitFee` 값이 없으면 그 열 자체를 표에서 숨기도록 수정 + 표 행 React key 중복(쿠팡 2행 모두 "쿠팡") 수정. 재배포 후 가전 4페이지 회귀 없음까지 재확인. G마켓·11번가 공식 환불 소요일(영업일 3~5일) 대조 완료.
- **Tier2-1 콘텐츠 보강 완료(2026-08-20)**: 쿠팡 와우 멤버십 2개(해지·환불) + 구독 "멤버십 해지" 계열 7개(넷플릭스·유튜브·네이버플러스 2개·티빙·웨이브·디즈니+)에 FAQ 추가, 총 9페이지. `coupang/wow-membership-refund`는 초안 작성자가 실제 페이지를 못 보고 추정으로 쓴 항목이라 배포 전 실제 페이지를 열어 흐름 대조 — "환불 문의" 같은 별도 메뉴는 없고 [해지하기] 버튼 하나로 조건에 따라 자동 환불 표시되는 구조임을 확인해 FAQ 문구를 실제 흐름에 맞게 수정 후 반영. **배포 직후 검증 관련 발견**: OpenNext 엣지 캐시(`enableCacheInterception`)가 이미 방문했던 URL을 재배포 후에도 한동안 구버전으로 서빙하다가, 요청이 들어오면 자동 재검증되어 새 콘텐츠로 갱신됨(수 회 요청 내 해소, 실서비스에 지장 없는 수준) — 다음부터 배포 직후 검증 시 이 지연을 감안하고 서버 응답(fetch no-store)으로 재확인할 것.
- **Tier2-2 콘텐츠 보강 완료(2026-08-20)**: 택배 3사(CJ대한통운·한진·롯데택배)의 배송조회·배송완료인데 상품 없음 6개, 카드 3사(KB·신한·삼성)의 재발급·모르는 결제 6개, 통신 2사(KT·LG유플러스)의 휴대폰 분실·정지 2개 — 총 14페이지에 FAQ 추가. 초안 작성자가 실제로 못 열어본 신한·삼성카드/KT·LGU+ 페이지는 배포 전 실제 barocheori.com 페이지 + 웹검색으로 대조: KT는 앱 이름이 "마이케이티"가 맞고 로그인 실패 시 대체번호는 초안의 "100"이 아니라 기존 페이지의 1588-0010/114로 정정, LG유플러스는 초안의 "U+멤버스" 대신 사이트 전체에서 이미 쓰던 "당신의 U+" 앱 이름 및 기존 번호(080-019-7000)로 맞춤 — 신한·삼성카드는 톤·번호 구조가 KB국민카드와 동일해 초안 그대로 반영. 작업 중 초안 정리 실수 하나 발견·수정: 신한·삼성카드의 "SOL페이/삼성페이 결제도 신고 방식 같나요" 질문을 처음에 `card-reissue`(재발급)에 잘못 넣었다가 `unrecognized-charge`(모르는 결제)로 옮김. 배포 후 서버 응답(fetch no-store)으로 14페이지 전체 FAQ 개수 일치 확인 + 3페이지 브라우저 렌더링·콘솔 오류 0 확인.
- **쿠팡 와우 해지 스크린샷 가이드 파일럿 완료(2026-08-20)**: `/company/coupang/wow-membership-cancel`에 실캡처 이미지 7장 + 캡션 + 주의박스 + 강조 처리로 "화면 그대로 따라하기" 섹션 신설. `ServiceTask`에 `screenshotGuide`/`guideCheckedAt` 필드 신설(다른 서비스에도 재사용 가능한 구조), HowTo JSON-LD 추가. 이미지 7장 전부 개인정보 가림 처리 확인 후 반영. next/image가 Cloudflare Images 바인딩으로 정상 최적화(webp)되는 것까지 배포 후 확인.
- **메인페이지 전면개편 스펙 v2 완료·배포(2026-08-20)**: 시안 2(틸 & 코랄) 색상 전면 교체(globals.css `@theme`에 `primary-*`/`accent` 토큰 신설, blue-* 클래스 12개 파일 전수 치환, 하드코딩 hex·그라디언트 교체) + Pretendard Variable 폰트 실제 로딩(CDN dynamic-subset 링크 추가, 이전엔 선언만 있고 로딩 코드가 없어 시스템 폰트로 보이던 문제) + 메인 헤드라인 "복잡한 ARS 싫으셨죠?"로 교체·배지 제거·통계 축소 + "급한 문제" 카드 4개를 진짜-급박 주제에서 수요-큰 주제(쿠팡 와우 해지·가전 수리비·해지후재결제·카드분실)로 교체, 카드를 히어로 옆에서 아래 가로 4열로 이동 + 모바일 체크리스트 8개 항목(터치 타깃 44px, 드롭다운 패널 스크롤, 전화 버튼 확대 등) 반영. 커밋 단위로 5단계 진행 후 main 병합·배포. **환경 이슈 기록**: 이 세션에서 Playwright 크롬 설치가 관리자 권한 필요로 실패, 내장 브라우저 도구의 화면 표시(Browser pane)도 간헐적으로 안 먹혀 스크린샷 대신 서버 HTML/CSS 텍스트 직접 검사로 검증(색상 hex, 클래스 존재 여부, FAQ/HowTo JSON-LD 개수, 가로스크롤 등) — 다음 세션도 스크린샷이 막히면 이 방식 사용. 뷰포트 리사이즈도 같은 탭에서 재탐색 후 간헐적으로 390→570 되돌아가는 현상 있었음(코드 문제 아님, 도구 환경 문제로 판단).
- **스크린샷 가이드 데스크톱 레이아웃 수정·배포(2026-08-20)**: 폰 세로 캡처가 데스크톱에서 본문 폭 전체로 렌더링돼 이미지 1장이 화면 2~3개 분량이던 문제 수정. sm 이상에서 각 단계를 좌우 배치(왼쪽 이미지 340px 고정, 오른쪽 숫자 배지+캡션 세로 중앙 정렬), sm 미만은 기존 풀폭 세로 스택 유지. 이미지에 박힌 코랄 배지와 중복이던 페이지 오버레이 검은 숫자 배지 제거, 대신 데스크톱 캡션 옆에 코랄(bg-accent) 숫자 배지로 대응시킴. 로컬 dev 서버 JS 실측(1280px: flex·340px·세로중앙 7단계 전부 확인 / 375px: 세로 스택·배지 숨김·가로스크롤 없음·콘솔 오류 0) 후 배포, 프로덕션 HTML no-store 검사로 새 클래스 서빙 확인. Browser pane 스크린샷은 이번에도 안 떠서(pane 미표시) JS 계산값 실측으로 대체 — 검증엔 지장 없음.
- **전면개편 v2 스펙 대비 전수 검증 + 잔여 버그 2건 수정·배포·전체 푸시(2026-08-20)**: 스펙 v2가 이미 전부 구현·배포된 상태임을 확인 후 검수로 전환 — 파랑 계열 잔재 0건(소스+public 에셋 전수, 유일한 매치는 globals.css 설명 주석), 히어로 카피·카드 4개·배지 제거·통계 축소·섹션 제목 전부 스펙 문구와 일치, 금지 문구(미출시 기능 암시) 0건, 메타데이터 불변 확인. 검수 중 **실버그 발견·수정 2건**: ① 상세 페이지 grid 컬럼에 `min-w-0`가 없어 수리비/환불 표의 `min-w-[32rem]`이 모바일 단일 컬럼을 554px로 밀어냄 → 본문 문장이 화면 밖에서 잘림(표 있는 페이지 전부 영향, lg에선 `minmax(0,…)` 덕에 정상이라 발견이 늦었음. 정적 스샷을 눈으로 봐서 잡은 버그 — DOM 수치 검증만으론 못 잡았을 것) ② 드롭다운 '닫기' 버튼 터치 타깃 28px→44px(315f192에서 이 버튼만 누락). **Playwright 크로미움 설치 성공**(`npx playwright install chromium` — chrome과 달리 관리자 권한 불필요, `%LOCALAPPDATA%\ms-playwright`): 이제 `npx playwright screenshot --browser=chromium --viewport-size="390,844" <URL> <파일>`로 실스크린샷 검증 가능. Playwright MCP 서버는 chrome 채널 고정이라 여전히 못 씀 — CLI로 찍고 Read로 확인하는 방식 사용. 검증 세트 5페이지(홈·검색·와우해지·수리비·카드분실) 1280/390 촬영 완료. **git push 완료**: 미푸시 9개 + 수정 커밋까지 전부 origin/main 반영, `git log origin/main..main` 비어 있음 확인. homepage-redesign-v2 브랜치는 87c97ee에서 이미 main에 머지된 상태(추가 머지 불필요).
- **디자인 개선 스펙 v3 "글자 먼저, 박스 다이어트" 전면 적용·배포·푸시(2026-08-20)**: 시안 2 색·카피는 유지하고 읽히는 방식만 전면 교체. ① globals.css `@theme`에 v3 토큰(ink-900~500 토스 회색 스케일, line/line-soft/bg-soft, primary/-strong/-soft, warn-*, danger, success + display~cta 타이포 토큰) — 스펙은 tailwind.config를 지목했지만 이 프로젝트는 Tailwind 4 CSS-first라 @theme이 동일 기능이라 거기 정의. body 17px/1.65 #4e5968, :lang(ko) keep-all, 포커스 링 #0f766e, .tnum 유틸. ② 전역 치환 grep 0건 확인: font-black/900 0, text-xs·12~13px 0(푸터 법적 고지도 14px로 올려 예외 불필요), 텍스트 slate/gray-400·500 0(다크 배경만 gray-300/ink-500로 수동 보정). ③ 메인: 헤더 56px+커피 pill(플로팅 커피 버튼 제거), 드롭다운 6개→칩 한 줄(데스크톱 40px/모바일은 히어로 안 가로스크롤 칩+바텀시트, 2×3 그리드 삭제), 히어로 display 토큰·"완전 무료입니다" 별도 줄 배지, 카드 모바일 2열(아이콘 사각형 제거), situationTasks는 ListRow(행 80px, 2열). ④ 상세: 골격 재배치(빵부스러기→H1→확인일 배지→왼쪽 4px 바 요약→준비물 칩→StepList→주의 콜아웃→스샷 격자→표→FAQ 아코디언), 읽기 컬럼 정확히 680px, StepList 번호 원 36px+연결선+꼭확인 코랄 원·라벨(밑줄·중첩박스 0), StepText가 데이터의 →를 렌더 시 " > "로 변환+[대괄호]만 굵게, 모바일 하단 고정 CTA 56px(스크롤다운 숨김+연락처 블록 IntersectionObserver), HowTo JSON-LD를 텍스트 단계 기준으로 재구성(guide steps의 howToImage로 대표 이미지 연결). ⑤ ScreenshotGuide v2: 타입 확장(n/img/thumb/label/caption/alt/linkedStep/howToImage/warning, checkedAt·basis), 모바일 3열 격자+<dialog> 라이트박스(이전/다음/닫기 48px, 스와이프·키보드, 단계 문장의 "화면 보기" data-guide-open 위임 연동), 넷플릭스 해지 텍스트 6단계(꼭확인 3·5)+실캡처 7장, 삼성 수리비 3·4단계 구체화+4장(zip 해제→public/images/guides/, zip 삭제), 쿠팡 7장은 thumb 없어 object-cover object-top 임시. **검증 실측**: 홈 390 첫 화면에 헤드라인+검색창+카드 노출(카드 top 498px), 와우 페이지 높이 8,586→**4,069px**, 격자 518px(≤600), 넷플릭스 4,180px·삼성 4,802px(표+격자 포함 페이지라 기준 외), 탭 타깃 44px 미달 0(데스크톱 전용 칩 40px은 스펙 4-1 명시값), weight900 0·최소 폰트 14px·라이트박스 전 기능(React 렌더가 비동기라 동기 스크립트 측정 시 카운터가 안 변한 것처럼 보임 — await 넣고 재측정하면 정상), 빌드 142p, 배포 후 4페이지 200·콘솔 0·이미지 깨짐 0. main 머지(b4b2fc8)·push 완료, origin/main..main 0개.
- **스크린샷 피드백 v4 반영·배포·푸시 완료(2026-08-21)**: 스펙 v3 배포 후 유석 님이 웹·모바일 스샷 5장씩 보고 준 미세조정 5건, `콘텐츠-초안/바로처리_스크린샷피드백_v4.md` 기준. ① **카드 순서 인기순 재정렬**: `app/company/[slug]/page.tsx`의 전역 `servicePriority` 맵을 `콘텐츠_보강_우선순위.md`의 구글 트렌드 조사 결과로 재작성 — 쇼핑몰은 환불(1)>배송지연(2)>취소(3)>주문조회(4)>교환(5)>반품예약(6), 쿠팡 와우 멤버십 해지·환불은 전 카테고리 통틀어 최우선(우선순위값 음수로 고정), 가전은 수리비·보증기간(1)>서비스센터(2)>출장수리(3)>자가진단(4)(조사에서 "수리비"가 압도적 1위였던 걸 반영, 기존엔 자가진단이 1번이었음), 카드·택배·통신·구독은 기존 순서가 이미 데이터와 맞아 유지. 쿠팡의 "주문취소"와 "와우 멤버십 해지"가 우선순위값이 똑같이 1이라 배열 원래 순서(주문취소가 먼저 옴)로 밀리던 게 근본 원인이었음 — 값 자체를 분리해 해결. ② **모바일 업체 상세 2×3 압축 그리드**: 상위 6개만 제목+1줄 요약 카드로 압축(`grid-cols-2`, `line-clamp-2`/`line-clamp-1`), 6개 초과분은 그리드 아래 기존 풀카드 유지, 데스크톱은 전체 서비스를 기존 스타일 그대로 별도 렌더(반응형 `hidden`/`sm:hidden`로 두 블록 전환, DOM 중복은 이 프로젝트의 기존 반응형 관례). ③ **홈 검색창 placeholder**: "예: 삼성 세탁기 고장, LG 출장수리" → "예: 쿠팡 와우 해지, 넷플릭스 해지"로 교체, 모바일 390px 폭에서 캔버스 실측(텍스트폭 221px < 입력창 여유폭 247px)으로 줄바꿈·잘림 없음 확인. ④ **모바일 홈 서브헤드라인 줄바꿈**: `<br className="sm:hidden">`을 "…연락처까지," 뒤에 삽입해 390px에서 정확히 그 지점(2줄)으로 강제 — `getClientRects()`로 실제 줄바꿈 위치까지 확인, 데스크톱은 br이 `display:none`이라 기존 한 줄 그대로. ⑤ **사이드바 아코디언 "방법 절차 한눈에 보기" 신설**: `ScreenshotGuide.tsx`에 라이트박스 없는 격자 전용 컴포넌트 `ScreenshotGuideGrid`를 추가해 `data-guide-open` 속성으로 클릭을 위임하는 방식으로 구현 — 같은 페이지에 이미 마운트된 본체 `<ScreenshotGuide>`의 dialog·전역 클릭 리스너를 그대로 재사용하므로 라이트박스 로직 중복 없이 썸네일 격자만 추가됨(콘텐츠 중복을 최소화하라는 스펙 취지 반영). "시작 전 준비" 카드 바로 위에 `<details>` 아코디언(기본 닫힘)으로 배치, 스샷 가이드 있는 페이지에만 노출(기존 조건 재사용), 데스크톱(`lg:block`) 전용으로 우선 적용. 브라우저에서 아코디언 펼침→다른 번호(n=3, n=5) 클릭→같은 다이얼로그가 정확한 스텝(이미지·캡션·카운터 일치)으로 열리는 것까지 실측 확인(첫 측정 시 React 비동기 렌더 때문에 이전 상태를 읽는 것처럼 보였던 건 재확인으로 해소 — 실버그 아님). 빌드(142p) 통과 후 배포, curl no-store로 프로덕션 5개 항목 전부 재확인(홈 placeholder/서브헤드라인, 쿠팡 카드 순서, 아코디언 RSC 페이로드 존재).
- **수정 스펙 v5 "임의 연결 제거 + 스샷 처리순서 정합성" 전면 반영·배포·푸시(2026-08-21)**: `콘텐츠-초안/바로처리_수정스펙_v5.md` 기준. 유석 님 피드백 2건("해지했는데 또 결제됐어요"를 누르면 넷플릭스로, "카드를 잃어버렸어요"를 누르면 KB국민카드로 지멋대로 감 / 쿠팡 와우 해지 처리순서가 스샷 7장과 안 맞고 "중간까지만 스크롤" "파란 유지하기 함정"이 텍스트에서 사라짐)에서 출발.
  - **원칙 2개 신설(앞으로 계속 지킬 것)**: ① 링크 문구에 업체명이 없으면 절대 특정 업체 상세로 직행하지 않고 상황 허브(`/problem/...`)를 거친다. ② 스샷이 있는 페이지는 "처리순서 번호 = 스샷 번호"가 1:1.
  - **상황 허브 10개 신설**: `app/data/problems.ts`(허브 데이터 + 빌드 타임 assert) + `app/problem/[problemSlug]/page.tsx`. 슬러그는 charged-after-cancel·lost-card·repair-cost·subscription-cancel·lost-phone·delivery-tracking·return-refund·termination-fee·card-reissue·appliance-broken. 화면 순서는 빵부스러기 → h1 → 확인일 배지 → lead → "업체를 고르기 전에" 콜아웃(firstAid) → 업체 선택 목록(첫 행에 `많이 찾는 곳` 배지, 탭 56px, 모바일 1열·데스크톱 2열) → fallback 회색 박스 → FAQ 아코디언 → "이런 상황도 있어요". 새 디자인 시스템 만들지 않고 상세 페이지 컴포넌트·v3 토큰 그대로 재사용, 읽기 폭 680px. JSON-LD는 BreadcrumbList + ItemList + (FAQ 있을 때) FAQPage. **부수 효과**: "카드 분실신고", "구독 해지했는데 결제됨" 같은 업체명 없는 검색어를 받을 페이지가 사이트에 처음 생김.
  - **빌드 타임 가드레일 2개**: `problems.ts`의 `assertProblemOptions`(중복 slug·옵션 1개짜리 허브·존재하지 않는 업체/업무 + 내가 추가한 fallback 링크·related 허브 검사)와 `page.tsx`의 `assertHomeEntries`(업체명 없는 문구가 업체로 직결되면 빌드 실패). **가드레일 실동작 확인 완료** — 일부러 `{ kind: "company", companySlug: "netflix", question: "해지했는데 또 결제됐어요" }`를 넣어 빌드하니 `[home] "해지했는데 또 결제됐어요"에는 업체명이 없는데 넷플릭스로 직결됩니다`로 빌드가 깨졌고, 확인 후 되돌림.
  - **홈 배선 교체**: `quickStarts`/`situationTasks`를 `HomeEntry` 유니온 타입(`kind: "company" | "problem"`)으로 바꿔 원칙 1을 타입·assert로 강제. 카드 4개 중 3개 + 상황 6줄 전부 `/problem/`으로, 쿠팡 와우만 직결 유지(문구에 "쿠팡"이 있음). 업체 직결 진입점만 부제 끝에 `· {업체명}`을 붙이고 허브에는 안 붙임.
  - **렌더러 버그 수정(문제 B의 근본 원인)**: 상세 페이지가 "화면 보기" 버튼을 `guideSteps.find(g => g.linkedStep === index+1)`로 찾아 **첫 장만** 집었음 → `filter`로 교체하고 `.map()`으로 연결된 장 수만큼 렌더. 2장 이상이면 `화면 보기 ③`처럼 스샷 번호(원문자)를 붙이고 `aria-label="{n}번 화면 보기"` 부여.
  - **3개 페이지 단계 교체**: 쿠팡 와우 해지 5→**8단계**(빠져 있던 설정(톱니바퀴) 단계 복원, 5번에 "중간까지만 스크롤하세요", 7번에 "파란 [유지하기]가 아니라 흰색 [해지 신청 완료하기]" 함정을 **텍스트로** 복원, keyStepIndexes [0,4,6]) / 넷플릭스 해지 6→**8단계**(keyStepIndexes [0,4,6]) / 삼성 수리비 5→**7단계**(keyStepIndexes [0,2,6]). linkedStep은 쿠팡·넷플릭스 1~7 그대로 1:1, 삼성은 3/4/5/6. 5번 스샷의 `warning`은 라이트박스에도 그대로 두어 이중 안전장치.
  - **모바일 아코디언**: v4에서 데스크톱 사이드바에만 있던 "방법 절차 한눈에 보기"를 모바일에서는 처리 순서 h2 바로 위에 접힌 채로 추가(`lg:hidden`, 기존 `ScreenshotGuideGrid` + `data-guide-open` 위임 그대로 재사용 — 라이트박스 로직 중복 0).
  - **검색·역방향 링크**: `findProblemMatches()`가 기존 `similarity()`와 `intentAliases`를 그대로 재사용해 검색 결과 맨 위에 허브 한 줄(`카드를 잃어버렸어요 — 카드사 고르기 ›`). 상세 페이지 "관련 업무" 제목 옆에 그 업무를 담은 허브로 되돌아가는 링크(`어디에서 결제됐나요? → 해지했는데 또 결제됐어요`).
  - **검증 실측**: 빌드 142→**152페이지**(스펙 예측치와 일치), sitemap 136→146. Playwright MCP는 여전히 chrome 채널 고정이라 못 씀 → `D:\claude-temp\pw`에 playwright를 설치하고 chromium으로 스크립트 실행(이 방식이 지금까지 중 가장 편함, 다음에도 이걸 쓸 것). 390/1280 양쪽에서 **쿠팡 7 / 넷플릭스 7 / 삼성 4개 "화면 보기" 버튼을 1번부터 끝까지 전부 클릭**해 라이트박스 카운터·이미지가 같은 번호로 열리는 것 확인, 처리순서에서 못 여는 스샷 0장, 허브 10개 전부 200·h1·확인일·업체 목록·탭 72.8px·가로스크롤 없음, 허브 내부 링크 87개 + 홈 링크 39개 전수 200, 굵기 800+ 0건·14px 미만 0건·탭 44px 미달 0건, 콘솔 오류 0. HowTo JSON-LD도 쿠팡 8·넷플릭스 8·삼성 7단계로 재생성 확인.
  - **배포 이슈 기록**: `npm run deploy`가 `.open-next` 삭제 EPERM으로 한 번 실패 → **dev 서버가 떠 있으면 폴더가 잠긴다.** dev 서버를 끄고 `.open-next`를 지운 뒤 재실행하면 됨. 배포 직후 새 URL 3개가 간헐 404였다가 1~2분 내 전부 200으로 안정화(엣지 자산 전파 지연, 실서비스 문제 아님) — 배포 직후 404는 바로 실패로 판단하지 말고 재확인할 것. 프로덕션에서 홈→허브→업체 클릭 흐름과 3개 페이지 화면 보기 전수 재검증 완료.
- **수익화 벤치마크 스펙 v6 1·2장 반영·배포·푸시(2026-08-21)**: `콘텐츠-초안/바로처리_수익화_벤치마크_스펙_v6.md` 기준. 애드센스 신청 사전 준비(1장)와 조사에서 확인된 UX 개선(2장)을 한 번의 배포로.
  - **1장 애드센스 사전 준비 — 전부 "잠든" 상태**: ① `/privacy`에 "광고와 쿠키" 섹션 신설(스펙 문구 그대로, adssettings·광고정책 링크 포함) ② `SiteFooter`와 `/privacy` 하단에 문의 이메일 `kysoboe@gmail.com`(나중에 전용 메일로 바꿀 때 이 두 곳만 고치면 됨) ③ `app/components/AdSlot.tsx` 신설 — `NEXT_PUBLIC_ADS === "on"`이 아니면 `null` 반환이라 빈 박스가 절대 안 생김. **광고 금지 구역 4가지(콜아웃·처리순서 리스트 내부 / 전화·공식 버튼 인접 / 상세 페이지 모바일 anchor / 홈 첫 화면)를 파일 주석으로 박아둠** ④ 배치 자리 4곳만 심어둠(상세: 처리순서 끝난 뒤 `in-article-1`, FAQ 끝난 뒤 `in-article-2`, 사이드바 `sidebar-1` / 허브: 업체목록 끝난 뒤) ⑤ `layout.tsx` head에 AdSense 코드 삽입 지점 주석. **ads.txt는 pub ID 없이는 의미 없어 만들지 않음**(공식: 코드/메타태그/ads.txt 중 택1).
  - **2장 UX**: ① **점프 목차**(`app/components/JumpNav.tsx`) — 요약 콜아웃 바로 아래 밑줄 링크 한 줄. **그 페이지에 실제로 있는 섹션만** 렌더하도록 호출부에서 조건 배열로 계산(거짓 0% 원칙). 섹션에 `#steps`/`#screens`/`#price-table`/`#faq` id + `scroll-mt-20`(헤더 56px 회피). 전화·공식 링크는 기존 `#contact-block` id를 그대로 씀(FixedBottomCTA의 IntersectionObserver가 이 id를 잡고 있어 이름을 안 바꿨음 — 스펙의 `#official` 대신). 갈 곳이 1개뿐이면 목차 자체를 렌더 안 함. ② **PageFeedback**(client) — FAQ 뒤 "이 안내가 도움이 됐나요?" + 버튼 2개(48px), GA4 `page_feedback` 이벤트(`helpful`, `page_path`). **layout의 `<GoogleAnalytics>`가 `window.gtag`를 전역 함수로 만드는 것 실측 확인**(`typeof window.gtag === "function"`), 그래도 차단기 대비 `dataLayer` 대체 경로 + 둘 다 없으면 무동작. 클릭 후 같은 높이 한 줄로 교체해 밀림 0. ③ **홈 다크 밴드(01/02/03) 삭제** → `/about`의 "바로처리가 일하는 방식" 섹션으로 이관(내용 그대로 보존). 홈 맨 아래 흰색 신뢰 섹션은 유지. ④ **확인일 배지** → "정보 확인일 2026-08-20 **· 바로처리 직접 확인**"(상세·허브 둘 다 — 나란히 보는 화면이라 한쪽만 바꾸면 버그로 보임), 상세·허브 하단 관련링크 위에 면책 한 줄(`text-caption text-ink-600`, 박스 없음). ⑤ **`app/search/page.tsx` v3 토큰 전면 교체** — slate/amber/primary-700 잔재 0건, 그림자·translate 제거, warn 토큰으로 빈 결과 박스 통일.
  - **부수 수정 2건**: `/privacy`에 "별도의 방문 분석 도구나 광고 쿠키도 사용하지 않습니다"라고 적혀 있었는데 **GA4가 실제로 돌고 있어 사실과 달랐고** 새 광고·쿠키 섹션과도 충돌 → "Google Analytics로 익명 집계만"으로 정정. 그리고 새로 넣은 privacy 인라인 링크 3개가 16~20px밖에 안 돼 탭 규칙 위반 → `-my-2.5 inline-flex min-h-12`(기존 "화면 보기" 버튼과 같은 패턴)로 48px 확보.
  - **3장 "안 하기로 한 것"은 하나도 건드리지 않음**(다크모드·공유 버튼·가로 스크롤 큐레이션·본문 중간 관련글 분산·순위 배지·홈 첫 화면 광고·wikiHow 모방).
  - **검증 실측(개발·프로덕션 양쪽, 390/1280)**: 빌드 152페이지 유지, 빌드 산출 HTML에 `data-ad-slot` **0건**, 실사이트 6개 URL에도 0건, 빈 박스 0개. 점프 목차 — 쿠팡 와우(스샷O·표X) 4항목 / KB 재발급(스샷X·표X) 3항목·"화면 따라하기" 없음 / 삼성 수리비(스샷O·표O) **5항목** / 쿠팡 반품(스샷X·표O) 4항목 / 허브 lost-card 3·lost-phone 2·delivery-tracking 2, **모든 링크 target 존재·탭 48px·클릭 후 착지 top ≥ 56px(헤더에 안 가림)**. 피드백 위젯 — dataLayer에 `["event","page_feedback",{helpful,page_path}]` 적재 확인 + **google-analytics.com `/g/collect` POST에 `page_feedback` 실려 나가는 것까지 네트워크로 확인**, 위젯 높이 113→113·문서 높이 불변(밀림 0). 검색 페이지 옛 클래스 0건(3개 질의 × 2뷰포트). 회귀 — 굵기 800+ 0·14px 미만 0·가로스크롤 0·콘솔 오류 0, 탭 44px 미달은 데스크톱 카테고리 칩 40px뿐(v3 스펙 4-1 명시값, 이번 작업과 무관).
  - **스펙과 다르게 간 곳 1가지(의도적)**: 체크리스트에 "스샷 있는 페이지(쿠팡 와우)에선 5개 항목"이라고 적혀 있으나 쿠팡 와우에는 참고표가 없어 4개가 맞음. 5개를 띄우면 없는 섹션 링크가 되어 스펙 본문의 "실제로 있는 섹션만" 규칙을 어김. 5개가 나오는 건 스샷+표를 다 가진 삼성 수리비이고 실제로 5개 확인됨.
- **마무리 스펙 v7 "디자인 통일 + 썸네일" 반영·배포·푸시(2026-08-21)**: `콘텐츠-초안/바로처리_마무리스펙_v7.md` 기준. 기획실장이 실사이트를 computed style로 재측정해 **v3 토큰이 홈·상세에만 적용돼 있고 나머지 페이지는 옛 디자인 그대로**임을 발견한 데서 출발(업체 페이지 h1 48px, privacy·about·information-policy h1 36px). 애드센스 심사원이 privacy·about을 반드시 열기 때문에 신청 전에 정리.
  - **업체 페이지(23개)가 핵심**: h1 48px → `text-h1 md:text-h1-md`(24/30px, 상세와 동일). **카드가 두 종류로 갈라져 있던 것을 하나로 통합** — v4에서 모바일 압축 그리드를 넣을 때 상위 6개만 신형 카드로 바꾸고 나머지·데스크톱은 구형(`rounded-2xl` + `shadow-sm` + `hover:-translate-y-0.5` + `hover:shadow-md` + `border-gray-200`)이 남아 한 화면에 그림자 있는 카드와 없는 카드가 공존했음. **`app/components/ServiceCard.tsx`를 새로 만들어 세 분기(모바일 압축 그리드·모바일 나머지 목록·데스크톱 그리드)가 같은 껍데기를 쓰게 함** — 다음에 또 갈라지지 않게 하는 게 목적. variant는 밀도(줄 수·CTA 한 줄)만 바꾸고 테두리·모서리·배경·hover·타이포 토큰은 완전히 동일. 들썩임 효과는 되살리지 않음.
  - **`/privacy`·`/about`·`/information-policy`·`not-found`**: 매핑표 적용 + 읽기 폭 680px(`max-w-[42.5rem]`), 버튼도 탭 48px 토큰 버튼으로.
  - **SiteFooter**: 다크 배경은 의도된 디자인이라 유지. 다크 위에 얹혀 있던 밝은 `border-gray-200` → `border-gray-800`, `text-lg` → `text-h3`, 본문색을 `gray-300`으로 통일(`ink-500`은 밝은 배경 전용 토큰이라 제거).
  - **잔여 옛 클래스까지 정리**: `BoldText`의 기본 `strongClassName`, `CategoryFinder`·`ScreenshotGuide`의 스크림(`bg-slate-950/30`, `backdrop:bg-slate-950/70`), 그리고 **`layout.tsx` body의 `text-gray-950`이 globals.css의 body 색을 계속 덮고 있던 것**을 전부 토큰으로. 결과: SiteFooter 다크 팔레트를 빼면 `gray-`/`slate-` 클래스 **0건**.
  - **쿠팡 와우 썸네일 7장**: zip을 `public/images/guides/`에 풀고 `coupang.ts`의 `screenshotGuide.steps` 7개에 `thumb` 필드만 추가(caption·alt·warning·linkedStep·howToImage는 손대지 않음). 격자에서 `object-top` 폴백이 사라져 **1·3·5·7번 썸네일에 실제로 눌러야 할 대상**(마이쿠팡 아이콘 / 와우 멤버십 행 / 월회비 결제수단 아래 [해지하기] / 파란 유지하기+흰색 해지 신청 완료하기)이 보임. 격자 높이 517px(≤600 유지).
  - **허브 breadcrumb**: position 1(홈)과 2(상황별)가 같은 URL이던 것 → 홈·문제 두 단계로. 화면 빵부스러기 세 칸은 그대로.
  - **검증 실측(개발·프로덕션 양쪽, 390/1280)**: 빌드 152페이지 유지. **h1이 모든 페이지에서 24px/30px로 동일**(상세 기준값과 일치 — 업체 48→24, privacy·about·info-policy 36→24, 404 포함), h2 20/22px. **14px 미만 0건 · 굵기 800+ 0건 · 옛 클래스 0건 · 가로 스크롤 0 · `[data-ad-slot]` 0개**(플래그 off 유지, 빌드 산출 HTML에도 0건). 탭 44px 미달은 데스크톱 카테고리 칩 40px 6개뿐(v3 스펙 4-1 명시값). **업체 23개 전수 검사** — 카드 스타일 시그니처(`12px|rgb(229,232,235)|none`)가 페이지마다 정확히 1종, 그림자 카드 0개, 최소 카드 높이 75px. 사이트맵 146개 URL 전부 200, 썸네일 7장 전부 200.
  - **스펙 범위를 살짝 넘어선 판단 2가지(의도적)**: ① 업체 페이지 하단 안내문이 `bg-primary-50` 박스였는데, 같은 문장이 상세·허브에서는 박스 없는 면책 한 줄로 나와서 "홈→업체→상세가 같은 사이트로 보인다"는 이번 스펙의 합격 기준과 어긋났음 → 상세·허브와 같은 한 줄 형태로 맞춤. ② 모바일 "나머지 목록" 카드도 압축 variant로 바꿈 — 스타일은 이미 같았지만 밀도가 달라 한 화면에서 두 종류로 보였음(모바일에서만 `순서대로 확인하기 →` 한 줄이 빠짐, 데스크톱은 그대로).
  - **손대지 않은 것**: `SiteHeader`(스펙 진단표에 없었고 14px 이상이라 규칙 위반 아님), `search/page.tsx` 72행 `text-lg`(아이콘용이라 스펙이 "그대로 둬도 된다"고 명시). **관찰 1건(미조치)**: 업체 카드 요약이 데이터 원문(`→`, `[대괄호]`)을 그대로 보여줌 — 상세 페이지는 `StepText`로 `>`·굵게 변환. 문구 렌더링 변경이라 이번 범위 밖으로 두고 기록만 함. **관찰 2건**: 공급받은 썸네일 2번은 원본에 박혀 있던 배지가 크롭에 함께 들어와 화면에서 번호가 두 번 보임(에셋 쪽 이슈, 코드 문제 아님).
- **스펙 v8 "모바일 후원 경로 복구 + 잔손질 2건" 반영·배포·푸시(2026-08-21)**: `콘텐츠-초안/바로처리_스펙_v8.md` 기준. 유석 님이 "모바일에서 후원 버튼이 잘 안 보인다"고 한 게 **안 보이는 게 아니라 아예 없던 것**이었음 — `CoffeeSupport.tsx` 루트가 `hidden sm:block`이라 640px 미만에서 렌더 자체가 안 됐고, 푸터에도 후원 흔적이 0건. 모바일 우선 사이트인데 대부분 방문자에게 후원 창구가 닫혀 있었음. (원인은 v3 4-7 "플로팅 커피 버튼 모바일 제거"인데, 의도는 본문 가리는 플로팅을 없애는 것이었지 대체 경로 없이 통째로 숨기는 게 아니었음.)
  - **복사 로직을 한 곳으로**: `app/components/useAccountCopy.ts` 신설 — `SUPPORT_ACCOUNT`(우리은행·김X석·1002-053-103-089)와 복사 훅을 두고, 헤더 pill(`CoffeeSupport`)과 새 푸터 블록이 **같은 코드**를 씀. 계좌를 바꿀 일이 생기면 이 파일 한 곳만 고치면 됨.
  - **푸터 후원 블록**(`app/components/SupportBlock.tsx`, `id="support"`): 모바일·데스크톱 공통 기본 경로. 본문을 가리지 않고 스크롤 끝까지 읽은 사람만 봄. 다크 배경이라 글자는 `gray-300` 체계, 계좌번호만 흰색. 복사 버튼 48px. **모바일 플로팅 버튼은 되살리지 않음.**
  - **`PageFeedback`**: "도움됐어요"를 누른 경우에만 "☕ 커피 한잔 후원하기"(`#support`로 점프, 새 창 안 엶) 한 줄 추가. **"아쉬워요"에는 노출 안 함** — 도움이 안 됐다는 사람에게 후원을 권하지 않음. state를 `boolean|null`로 바꾸고 `min-h-12` 유지해 레이아웃 밀림 0.
  - **겹침 방지**: 상세 페이지 하단 고정 CTA(`FixedBottomCTA`)가 기존 `#contact-block`에 더해 **`#support`도 함께 관찰**하도록 확장(IntersectionObserver 대상 배열화). 후원 블록이 화면에 들어오면 CTA가 숨어 가리지 않음. 페이지 중간에서의 기존 동작(내리면 숨고 올리면 나타남)은 그대로.
  - **잔손질 A**: `ServiceCard`의 `summary`를 `StepText`로 감쌈 — 업체 카드에서도 `→`가 ` > `로, `[대괄호]`가 굵게 렌더되어 상세 페이지와 같은 모양. 문구는 그대로. line-clamp 유지 확인(모바일 1줄 24px, 데스크톱 2줄 48px).
  - **잔손질 B**: 쿠팡 2번 썸네일 교체(원본에 박힌 코랄 ② 배지가 크롭에 들어와 숫자가 두 번 보이던 것). 파일명 동일이라 `coupang.ts`는 손대지 않음. 프로덕션 파일이 새 파일(15,620바이트)과 바이트 동일한 것까지 확인.
  - **검증 실측(개발·프로덕션 양쪽)**: 빌드 152페이지 유지. 홈·상세·허브·업체 **4종 페이지 × 390/1280에서 푸터 후원 블록 전부 노출**, 계좌번호 표시·복사 버튼 48px. **복사 실제 동작 확인** — 클립보드에 `1002-053-103-089`가 들어가고 라벨이 "계좌번호 복사됨 ✓"로 바뀜(푸터·헤더 pill 양쪽). 390px에서 헤더 pill은 부모가 `display:none`이라 여전히 안 보이고 **플로팅 후원 요소 0개**. **CTA 겹침 0** — 맨 아래·위로 50px 올린 상태 모두에서 CTA가 화면 밖(top 844 = 뷰포트 높이), 후원 블록은 화면 안. 페이지 중간에서는 CTA가 정상 재등장(top 763). 피드백 — 도움됐어요는 후원 링크 노출(48px), **아쉬워요는 `null`로 미노출**, 위젯 높이 113→113·문서 높이 불변. 회귀 — 14px 미만 0 · 굵기 800+ 0 · 가로 스크롤 0 · `[data-ad-slot]` **0개**(플래그 off 유지) · 콘솔 오류 0, 탭 44px 미달은 데스크톱 카테고리 칩 40px뿐(v3 명시값). 사이트맵 146개 URL 전부 200.
  - **배포 이슈**: `npm run deploy` 첫 시도가 `.open-next` 관련으로 한 번 실패했다가 재실행하니 정상 — dev 서버를 막 끈 직후에 폴더를 지우면 핸들이 남아 있는 듯. **재실행하면 됨**(v5 때와 같은 계열).
- **스펙 v9 "11번가 화석 링크 교체" 반영·배포·푸시(2026-08-21)**: `콘텐츠-초안/바로처리_스펙_v9.md` 기준. 유석 님이 폰으로 실사용하다 발견 — `/company/11st/*`에서 [공식 페이지 열기]를 누르면 "Mobile Station"·QR코드가 뜨는 **2000년대 화석 페이지**로 떨어졌음. 원인은 `www.11st.co.kr/OrderDelvInfo.page`를 11번가가 리다이렉트하지 않고 옛 페이지를 그대로 서빙하는 것 — **우리 코드 잘못이 아니라 링크 대상이 죽은 것**이지만, 사용자에겐 우리 사이트가 이상한 데로 보낸 것으로 보이므로 "죽은 링크 강한 기피" 원칙 위반 상태였음.
  - **`app/data/companies/elevenst.ts` 5개 업무 교체**(cancel·return-refund·order-check·delivery-not-received·return-pickup-delay): `officialUrl`을 `https://buy.11st.co.kr/my11st/order/OrderList.tam`(기획실장이 원격 실측 검증 — 비로그인 시 현행 로그인 페이지로 302 → 로그인하면 returnURL로 주문목록 복귀)로 교체, `officialLinkType: "login"` 추가(5개 다 없었음 → 화면에 "로그인 후 이어서 처리"로 표기됨), `officialNextStep` 추가(5개 다 없었음 — "로그인하면 주문 목록으로 이동합니다. 처리할 상품을 눌러 진행하세요."), `lastChecked` 2026-08-21로 갱신.
  - **`customer-center`의 `cs.11st.co.kr`은 손대지 않음** — 원격 확인에서 내용이 비어 보였지만 JS로 그려지는 최신형일 가능성이 높아 증거 없이 바꾸지 않는다는 스펙 방침. `lastChecked`도 2026-08-18 그대로. (이 페이지는 `getUsefulOfficialUrl`이 customer-center를 제외하므로 애초에 공식 버튼이 렌더되지도 않음.)
  - **검증**: 빌드 152페이지 유지·lint 통과, 빌드 산출 HTML에 화석 주소 **0건**·새 주소 5건. 실사이트 5개 페이지 전부 새 주소 1건·화석 0건·"로그인 후 이어서 처리" 표기·안내 문구 노출 확인. 앱 전체에 `OrderDelvInfo` 잔존 0건. **배포 직후 `delivery-not-received` 한 페이지가 잠깐 구버전을 서빙**했다가 재요청하니 바로 새 버전(`x-opennext-cache: HIT`) — 늘 있던 엣지 재검증 지연이므로 배포 직후 1회 결과로 판단하지 말 것.
  - **후속 예약**: 11번가에서 이런 게 나왔으면 다른 곳도 있을 수 있음 → 23개 업체 `officialUrl` 약 50개를 **실브라우저로** 여는 전수 점검을 다음 기획실장 세션에서 진행(원격 확인은 JS 사이트에서 오판함). 쇼핑몰·통신사처럼 옛 시스템 많은 곳부터. 발견분은 v10으로 묶음.
- 지금 단계: **색인 시작됨(2026-08-20 심야, 기획실장이 서치콘솔에서 직접 확인).** 홈 · `/company/coupang/wow-membership-cancel` · `/company/netflix/membership-cancel` · `/problem/subscription-cancel`(그날 만든 허브)까지 **Page is indexed**. `/company/samsung-electronics/repair-cost-warranty`와 `/problem/charged-after-cancel`은 색인 요청 접수 상태. → **애드센스 신청 방아쇠 조건은 이미 충족**됐고, v7 배포까지 끝났으므로 심사원이 보는 페이지(privacy·about)가 정리된 상태로 신청할 수 있음.
- **다른 세션(Claude 웹 채팅, 기획실장)에서 병행 완료된 항목** — 기획노트.md 참고, 이 문서에도 동기화: `www.barocheori.com` DNS 리다이렉트 추가 완료, GA4 ↔ 구글 서치콘솔 도메인 속성(`sc-domain:barocheori.com`) 연동 완료. 아래 "남은 할 일"에서 제거함.

### 남은 할 일 (우선순위순)
1. **콘텐츠 보강 계속**: 가전 4 + 쇼핑몰/구독/카드 14 + 쿠팡 와우·구독 해지 9 + 택배/카드/통신 14 = 41페이지 + 스크린샷 가이드 파일럿 1개 완료(`콘텐츠-초안` 폴더에 배치별 초안 보관 중, Tier2까지 소진). 다음은 Tier3(위약금·취소·교환·주문조회 등, 약 30개 안팎) — 관심도가 상대적으로 낮은 편이라 여기서 색인·트래픽 반응을 먼저 보고 이어갈지 판단하는 것도 방법. 스크린샷 가이드는 다음 파일럿 후보(넷플릭스 해지, 삼성/LG 수리비, 유튜브 프리미엄 해지)로 확장 가능.
2. **서치콘솔에 `/problem/charged-after-cancel` URL 검사·색인 요청 1건**(허브 10개 대표로) — 스펙 v5 체크리스트의 마지막 남은 항목, 대시보드 작업이라 기획실장(웹 채팅) 쪽에서 처리
3. 색인 확인 계속: `site:` 검색 + 서치콘솔/서치어드바이저 보고서(8/27 자동 점검 예약됨)
4. **애드센스 신청 — 지금 바로 가능.** 방아쇠(색인 시작)는 8/20에 충족됐고 v7까지 배포 완료. 코드 쪽 사전 준비는 v6에서 전부 끝났고, 남은 건 유석 님 계정 작업 15분(스펙 v6 6장 체크리스트 참고 — kysoboe@gmail.com 계정 확인 → 사이트 추가 → 코드 스니펫 받아 클코에게 전달 → 검토 요청). **승인 후 클코가 할 일**: pub ID 코드를 `layout.tsx` 삽입 지점에 넣기 → `NEXT_PUBLIC_ADS=on` + AdSlot 4곳에 in-article 코드 반영(자동광고 말고 수동 배치부터) → ads.txt 생성
5. **공식 링크 전수 점검(v10 후보)** — 11번가에서 화석 링크가 나온 만큼 23개 업체 `officialUrl` 약 50개를 실브라우저로 여는 점검이 필요. 원격 fetch는 JS 렌더 사이트에서 오판하므로 브라우저 필수. 쇼핑몰·통신사처럼 옛 시스템 많은 곳부터. 기획실장 세션에서 진행 예약됨
6. 쿠팡파트너스/인터넷 가입 CPA 제휴 (콘텐츠 보강 후)
7. ~~(선택) Playwright용 크롬 설치~~ → **해결됨(2026-08-20)**: `npx playwright install chromium`으로 관리자 권한 없이 설치 완료, CLI 스크린샷 검증 가능

---

## 1. 최우선 규칙 (다른 무엇보다 먼저)

- **배포는 오직 `npm run deploy`** (프로젝트 폴더에서 실행).
  - `git push`는 배포가 아니다. Cloudflare Git 자동 빌드는 현재 고장 상태("Latest build failed").
  - Netlify/Vercel CLI로 배포 금지.
- 운영자(유석 님)는 **코딩을 전혀 모른다**. 모든 안내는 복사-붙여넣기 가능한 명령/문장 + 클릭 단위 설명으로, 한 번에 한 단계씩, 전문용어는 쉬운 비유로.
- 코드 수정은 Claude가 직접 하고 완성본을 만든다. 운영자에게 코드를 고치게 하지 않는다.
- **돈이 드는 결정**(결제, 유료 플랜, API 구매)은 금액·이유·무료 대안을 먼저 제시하고 승인을 받는다. 자금 사정이 빠듯하다.
- 카드번호·비밀번호 입력은 절대 대행하지 않는다. 그 순간만 운영자에게 넘긴다.
- 파괴적 작업(대량 삭제, DNS 변경, 도메인/계정 설정 변경)은 실행 전 반드시 설명하고 승인받는다.
- **습관 규칙**: 유석 님과의 작업 세션이 끝날 때마다 위 "0. 최신 상태 요약" 섹션을 그날 결과로 갱신한다.

## 2. 프로젝트 개요

- 바로처리: 카드 분실, 배송 문제, 구독 해지, 통신 위약금, 가전 고장 같은 생활 문제를 "전화·ARS 이전에 직접 해결하는 공식 메뉴와 순서"로 안내하는 독립 서비스. 23개 업체 × 109개 업무 + 상황 허브 10개 = 152페이지(프리렌더 기준).
- 포지셔닝: 미국 GetHuman의 한국판 + "셀프 처리 우선, 전화는 마지막".
- 타겟: 검색 유입은 "급한 문제가 생긴 사람". 4050 부모 세대는 자녀가 카톡으로 공유하는 링크로 도달시킨다. 4050은 네이버 사용자 → 네이버 서치어드바이저가 구글 SEO만큼 중요.
- 수익: 단기 월 40만 원(애드센스+제휴), 장기 월 200만 원. "해지" 페이지 ↔ "갈아타기(인터넷 가입 CPA 등)" 수요 궁합이 숨은 수익 포인트.

## 3. 기술 스택 & 인프라

- Next.js 16.3.1 (App Router, TypeScript, Tailwind 4) + @opennextjs/cloudflare 1.20
- 호스팅: Cloudflare Workers 무료 플랜 · Worker 이름 `baro-cheori` · 계정 ID `75b97a57a6b5a30ef089c425bf955d7b`
- 도메인: barocheori.com (Cloudflare Registrar, 자동갱신 ON, 만료 2027-08-19)
- 저장소: GitHub `kysoboe-cell/baro-cheori`
- 사이트 주소는 `app/lib/site.ts`의 `SITE_URL` 한 곳에서 관리 (canonical/sitemap/robots 전체 제어). 환경변수 `NEXT_PUBLIC_SITE_URL`로 재정의 가능.
- **`open-next.config.ts`의 캐시 설정은 절대 비우지 말 것.** `staticAssetsIncrementalCache` + `enableCacheInterception`이 있어야 141개 프리렌더 페이지가 정적 자산으로 서빙된다. 비우면 기본값 `"dummy"`(아무 일도 안 하는 가짜 캐시)로 돌아가 요청마다 서버가 페이지를 처음부터 다시 그리고, Workers 무료 플랜의 요청당 CPU 한도(10ms)를 넘겨 503 + 링크 먹통이 재발한다.
- 네이버 소유확인: `NEXT_PUBLIC_NAVER_SITE_VERIFICATION` 환경변수가 layout.tsx에 연결됨 → `.env.production`에 값 추가 + 재배포로 반영.
- 분석: GA4 `G-L1B6W4F68T` (layout.tsx 하드코딩), 구글 서치콘솔 verification 메타 포함.

## 4. 운영자 로컬 환경

- Windows (명령 안내는 PowerShell 기준으로)
- C드라이브 용량 부족 → 새로 설치하는 것은 가능하면 D드라이브로
- 브라우저: 네이버 웨일 (Claude 확장 연결됨) · 에디터: VS Code + Claude Code 확장 · 플랜: Claude Max
- 프로젝트 폴더: `D:\Projects\baro-cheori` (D드라이브 이전 완료)

## 5. 자주 쓰는 명령

- 개발 서버: `npm run dev`
- 빌드 확인: `npm run build`
- 배포: `npm run deploy`  ← 유일한 배포 방법. 이후 모든 재배포도 항상 이 명령.

## 6. 작업 이력 상세 (2026-08-19 기준, §0 요약의 원본 기록)

- 완료: Workers 이전, 136페이지 라우팅 정상, 구글 서치콘솔(workers.dev 속성) 소유확인·sitemap 제출, GA4 실시간 수집, 도메인 구매 + Worker 커스텀 도메인 연결, 도메인 전환 코드 패치 제작·검증 완료.
- 완료(2026-08-19): **도메인 전환 배포.** ① `app/lib/site.ts` 기본 주소 → barocheori.com ② `next.config.ts`에 workers.dev → barocheori.com 영구 리디렉트(308) ③ `.env.production` 신설. 확인 3가지 전부 통과 — barocheori.com 정상 / workers.dev 접속 시 308 자동 이동 / sitemap.xml 주소가 새 도메인.
- 완료(2026-08-19): **503 링크 먹통 응급수리.** 원인 = ① `open-next.config.ts` 캐시 미설정으로 요청마다 페이지 재렌더링 ② next/link 프리페치가 한 번에 수십 개 발생 → 요청당 CPU 10ms 초과. 조치 = ① 정적자산 캐시 + 캐시 인터셉션 ② Link 25곳에 `prefetch={false}`. 검증 = 실제 브라우저로 링크 3개 클릭 정상·콘솔 오류 0 / 동시요청 40개 전부 200(503 0건) / 응답헤더 `x-nextjs-prerender: 1`.
- 완료(2026-08-20): **옛 workers.dev 주소 완전 차단.** `wrangler.jsonc`에 `"workers_dev": false` 추가 → 배포. barocheori.com 커스텀 도메인 연결은 변경 없음. 검증 — `baro-cheori.kysoboe.workers.dev` 접속 시 404 "Page not found"(Cloudflare 기본 화면, 308 리디렉트가 아니라 아예 서빙 중단) / `barocheori.com` 홈+링크 3개(카드분실, 쿠팡배송, KT분실폰) 전부 200 정상. → §9 Workers 주소 항목은 "폐쇄됨"으로 갱신, §7 신호 분산 문제 해소.
- **다음 = "청소의 날"** (한 세션에): ① 서치콘솔에 barocheori.com 도메인 속성 신규 등록(DNS TXT) + sitemap 제출 ② 네이버 서치어드바이저 등록 + 옛 netlify 항목 삭제 ③ 다음(Daum) 검색등록 ④ 넷리파이 사이트 삭제 ⑤ GA4 스트림 URL 갱신 + 새 서치콘솔 속성 연동.
- 이후 성장: 검색량 큰 20~30페이지(해지 위약금·반품·분실 계열) 콘텐츠 보강(실제 캡처·소요시간·실패 케이스·FAQ) → 애드센스 신청 → 쿠팡파트너스/인터넷 가입 CPA → "부모님께 카톡으로 보내기" 공유 버튼, FAQ/HowTo 구조화 데이터.

## 7. 알려진 문제

- 구글 검색 노출 없음: 2026-08-19 공개된 신생 사이트라 정상 범위 — 색인 대기 중(§0 참고). 넷리파이 중복·workers.dev 신호 분산은 이미 해소됨(아래 참고).
- ~~넷리파이 사본이 살아있고 자기 자신을 canonical로 선언 중~~ → **2026-08-19 밤 대시보드에서 사이트 삭제 완료.** GitHub 연동 자동배포 문제도 같이 해결됨.
- Cloudflare Git 자동 빌드 실패: 로컬 `npm run deploy`가 정상이라 급하지 않음. 나중에 Workers Builds 빌드 명령어 점검. 무료 한도는 월 3,000빌드분(현재 9분 사용)이라 여유 막대함.

## 8. 작업 원칙

우선순위: **정확성 > 검증 > 최소 변경 > 명확성 > 유지보수성**

- 파일·API·스키마가 존재한다고 가정하지 말고 먼저 읽어서 확인한다.
- 수정 전에 관련 파일을 읽고, 수정 후에는 빌드·실행으로 검증한다. 검증 없이 "성공했다"고 말하지 않는다.
- 요청된 작업에만 변경을 국한하고, 관련 없는 리팩토링은 하지 않는다.
- 가장 단순한 해결책을 선호하고, 불필요한 의존성·추상화를 추가하지 않는다.
- 기존 프로젝트의 관례와 스타일을 따른다.
- 막히면 멈추고: 무엇이 막혔는지, 무엇이 검증됐는지 명확히 보고한다.

## 9. 참조 주소

- 정본: https://barocheori.com
- Workers 주소(2026-08-20부터 완전 폐쇄, `wrangler.jsonc`의 `workers_dev: false`): https://baro-cheori.kysoboe.workers.dev — 이제 404만 뜸, 코드 수정할 필요 없음
- 넷리파이 옛 주소(2026-08-19 사이트 삭제 완료, 참고용): https://baro-cheori.netlify.app
- 도메인 갱신: 2027-08-19, $10.46 자동결제 (등록 카드) — 내년에 잊지 말 것
- Cloudflare 무료 한도 체감: 요청 884/일 (한도 10만) — 여유 막대함

@AGENTS.md
