바로처리 V10 적용 방법

1. 받은 ZIP 파일을 D:\Projects\baro-cheori 폴더 안에 넣습니다.
2. VS Code PowerShell에서 아래 명령어를 위에서부터 실행합니다.

cd D:\Projects\baro-cheori
Expand-Archive -Path ".\baro-cheori-search-v10-20260818.zip" -DestinationPath "." -Force
Remove-Item ".\baro-cheori-search-v10-20260818.zip"

npm run lint
npm run build

정상 기준: 빨간 오류 없이 빌드가 끝나고 정적 페이지 89/89가 표시됩니다.

git add .
git commit -m "improve plain language search and ARS-free guidance"
git push

이번 변경 내용
- 생활 말투 검색 강화
- 자동완성에 실제 해결 내용 미리 표시
- 검색 결과 없음 화면 개선
- 상세 화면에서 온라인 해결을 먼저 안내
- 긴급 상황이 아니면 전화·ARS를 마지막 수단으로 표시
- 공통 해결 문장을 짧고 쉬운 말로 재작성
- 로고는 변경하지 않음
